# {{titleCase package}}

This package is generated with plop for the {{> projectName}} project.

Created with: `plop react-typescript-package -- --component={{ component }} --package={{ package }}`

install with: `npm install @{{>projectName}}/{{lowerCase package}}`