/* eslint-disable */
import * as React from 'react';
import { storiesOf } from '@storybook/react';
import { {{ pascalCase component }}Dummy as data } from './{{ pascalCase component }}.dummy';
import { {{pascalCase component}} } from './{{pascalCase component}}';

storiesOf('{{pascalCase component}}', module).add('Default', () =><{{ pascalCase component }} {...data} />);